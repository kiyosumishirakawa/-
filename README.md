tone() 使用時は出力が高速にON/OFFするため、テスターでは5Vではなく約0.5Vとして表示された。停止中は0Vで、動作中はブザーが鳴っていたため、正常に出力されていると判断した。

- **操作**: テスターを導通モードにして、Arduino GND とブレッドボードのGNDライン、ブザーのGND側、ポテンショメータのGND側、74HC595のGNDピンの導通を確認した。
- **結果**: すべての測定箇所で導通音が鳴った。
- **正常？**: Yes
- **気づいたこと**: Arduino GND と各部品のGNDが共通になっていることを確認できた。GNDが共通でない場合、表示やブザー、ポテンショメータの入力が不安定になる可能性があるため、ハードウェア確認では重要な項目だと分かった。

### TC-HW3: 電源電圧の確認

* **操作**: Arduino の 5V ピンと GND ピンの間の電圧をテスターで測定した
* **測定値**: 約 4.47V
* **期待**: 4.8V〜5.2V の範囲
* **正常？**: No
* **気づいたこと**: 期待値より低い電圧が測定された。原因として、USB給電の電圧低下、接続している4桁7セグLEDやブザーなどの負荷、またはブレッドボードやジャンパ線による電圧降下が考えられる。今後、Arduino本体の5V-GND間とブレッドボード電源ラインの両方を測定し、どこで電圧が下がっているかを切り分ける。
